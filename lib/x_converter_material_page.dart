import 'package:flutter/material.dart';

class XConverterMaterialPage extends StatelessWidget{
  const XConverterMaterialPage({super.key});
  @override
  Widget build(BuildContext context){
    return const Scaffold(
      body: Center(child: Text('Hello, senade!!!'),
      )
    );
  }
}